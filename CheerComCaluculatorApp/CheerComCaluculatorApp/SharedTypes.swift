import Foundation

enum RotationDirection {
    case positive
    case negative
}

enum TransformMode {
    case position, rotation, scale
}

enum TransformDirection {
    case up, down, left, right
}
