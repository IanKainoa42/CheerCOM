# Rejecting task per AGENTS.md HARD STOP

The task "Continuously improve the app's realistic human 3D body model and its Center of Mass (CoM) calculation" is rejected based on the explicit `HARD STOP` directive in `AGENTS.md`.

This task represents a vague-scope "Mission" session without a specific failing test name, user-visible defect, or reference to prior PRs, which is strictly prohibited by `AGENTS.md`.

As instructed by `AGENTS.md`, I am stopping immediately and opening a PR that does nothing except explain the rejection. This represents the explicit, reviewable refusal required.

Note: The recurring session that generated this task must be cancelled in the Jules web UI to prevent further runs.